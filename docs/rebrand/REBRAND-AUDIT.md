# THOUGHTS COUNT — Complete Rebrand Audit (Architect) — TC-75

Repo: `C:\Users\David\Damay Enterprises\thoughtfulness`. Live: thoughts-count.netlify.app → thoughtscount.com. Static `public/index.html` (~4500 lines, vanilla JS + inline CSS) + `public/*.js` modules + `netlify/functions/*.mjs`. **No app build step** (only guides are generated). Brand tokens NOT centralized: FOUR independent `:root{}` palettes + ~130 hardcoded literals + drifting `var(--x,#fallback)` fallbacks.

## 1. Surface inventory
- **`public/index.html`** — landing + all modals + inline CSS (`:root` L35-51), `#bg` cloud gradient L64-79, two brand-logo SVGs (L616 header, L849 modal head).
- **`public/_orb.js`** — Della's canvas orb. Palette L34-41 pigments + rgba L110-160. Highest-risk visual change. 3 mount points.
- **`public/companion.js`** — sign-in, People home, cards, dates, memory, plan save. Brand SVGs L199 + L445; sent-badge SVG L519-522.
- **`public/_capture.js`** — capture toast/review (colors+strings L129-150).
- **`public/_memory.js`** — "things you've noticed" UI (strings/placeholder).
- **`public/roster.js`** — roster (brand SVG L101).
- **`public/import.js`** — import wizard (brand SVG L56).
- **`public/_inline-mic.js`** — mic button (L28-29, L102 clay).
- `public/_guided.js` / `_dates.js` / `_checkback.js` / `_relationship.js` — logic; strings only, no colors.
- **`public/privacy.html`** — own `:root` L11 (`--clay #a97350;--sage #7d8a68;--deep #454e43;--cream #fffbf4;--ink #34382f;--line #e7ddca`), Fraunces/Nunito L9, text wordmark L16/L29.
- **`public/terms.html`** — identical structure.
- **`public/guides/*/index.html`** (21) + **`public/guides/index.html`** hub — GENERATED by `scripts/build-guides.mjs` (templates `page()` ~L803, `hub()` ~L959). Do NOT hand-edit outputs.
- **`netlify/functions/_email.mjs`** — shared plan/reminder/nudge email layout. "🤍 Thoughts Count" wordmark L56, Georgia serif, hex L54-98, signature `HER_NAME` L17.
- **`netlify/functions/_digest.mjs`** — weekly report email. `const C={...}` colors ~L43, "🤍 Thoughts Count" L138.
- **`netlify/functions/_persona.mjs`** — `HER_NAME="Della"` L12 (do NOT rename in this rebrand — separate gated decision).

## 2. Brand-token audit (find-and-replace map)
### Fonts (Fraunces+Nunito → Montserrat) — FIVE loaders:
- `index.html` L33 link, L56 body Nunito, L81 h1/h2/h3/.serif Fraunces; inline uses L85,87,108,218,227,284,301,366,370,395,432,832 + JS template strings L1614,1660,1796,2927,3023,3169.
- `companion.js` L581 (`'Fraunces',Georgia,serif` blockquote).
- `privacy.html` L9 link + L13/16/17/18; `terms.html` L9 + L13/16/17/18.
- `scripts/build-guides.mjs` L870/L1005 links, L877/880/883 + L1009/1012/1015 font-family — then re-run `node scripts/build-guides.mjs`.
- **Emails = Georgia serif** (`_email.mjs` L56/63/80/96; `_digest.mjs` L83/124/138/180). Recommend KEEP a system/Georgia stack for email (clients don't load web fonts) — design call.
### `:root{}` blocks (4 independent copies):
- `index.html` L35-51: `--paper #dfeae6;--paper-2 #e5eee9;--cloud #f8fbf9;--ink #34382f;--ink-soft #5f6b60;--sage #7d8a68;--sage-deep #5f6c4c;--clay #c28a63;--clay-deep #a97350;--blush #e7efe9;--mist #dfeae1;--line #d9e4dd;` + 3 shadow tokens.
- `privacy.html` L11 + `terms.html` ~L11: `--clay #a97350;--sage #7d8a68;--deep #454e43;--cream #fffbf4;--ink #34382f;--line #e7ddca;` (DIFFERENT var names).
- `build-guides.mjs` L875 (guides) + L1007 (hub): `--paper #dfeae6;--cloud #f8fbf9;--ink #34382f;--soft #5f6b60;--sage #5f6c4c;--clay #a97350;--line #d9e4dd;`
### Hardcoded literals (~130, NOT via :root) — load-bearing groups:
- **`_orb.js`** pigments L34-41, render rgba L110-112, heart bloom L148-150, vignette L158-160 → RE-AUTHOR per Design-System §7 (not find-replace).
- **`index.html` inline SVG scene art:** how-it-works cards L705/710/715; trust icons L694-696; lightbulb scene L3738-3742; gift-tile gradient L3811 (`TILE_BG`) + stroke L3813.
- **`index.html` panel/orb gradients:** `.panel` L241; orb halo radial L449 + L822; state-tints L836/838/840; scrim L232.
- **`index.html` UI hexes:** L154,294,296,304,305,341,362,363,378,379,383,388,390,391,396,397,398,530,557,1113,1120; feedback pills L4094-4095/4114; JS-set L1168,1206,1208,1530.
- **Other JS:** companion.js L199/445/519-522/581; _capture.js L129-150; _inline-mic.js L28/102; roster.js L101; import.js L56.
- **Drifting `var()` fallbacks** (re-point to new tokens): index.html L780/788/789/790/795/796/797/1117/1539/1687 use hexes that DON'T match `:root` (`#c98a63` vs `--clay #c28a63`; `#8a9a72` vs `--sage #7d8a68`; `#7a7263` vs `--ink-soft`).
### Gradient: `index.html` L64-79 (`#bg` L65 + 6 `.cloud` L68-73 greens `#9fc596/#8fbfb4/#b3d2ac` + blues `#a2bdd8/#a9c3e2/#98bcd8`, drift keyframes L74-79, reduced-motion L80). Guides generator has its OWN static `#bg` (build-guides.mjs L878/L1010).
### Logo/mark:
- `public/favicon.svg` — `#c77b59`+`#788b62`. REPLACE.
- Two full inline brand SVGs `index.html` L616 (header) + L849 (modal head), `viewBox 0 0 172.03 155.18`, `#788b62`+`#c77b59`.
- Simplified heart SVGs (`viewBox 0 0 24 24`, `stroke #7d8a68` + circle `#c28a63`): `companion.js` L199+L445, `roster.js` L101, `import.js` L56, `build-guides.mjs` L919+L1039.
- Emails: text + 🤍 emoji (no SVG) — clients don't render inline SVG; keep text or attach PNG.
- Schema.org `logo` → favicon.svg: index.html L29.

## 3. Icon / favicon / social / PWA
- **favicon.svg** — index.html L16, privacy/terms, guides (build-guides L867/L1001), schema L29.
- **og.png** (1200×630) — index.html L22+L26; guides L862/866/894/998. Source template `scripts/og-image.html`.
- **apple-touch-icon: NONE. Web app manifest: DOES NOT EXIST. theme-color: NONE.** All net-new (home-screen icon needs a manifest).

## 4. AI-tell punctuation (TC-128)
Curly quotes / em / en dashes: NONE in user-facing app strings (raw hits are code comments — out of scope). **Ellipsis char `…` (U+2026) to FIX → `...` or reword:**
- `companion.js` L497,595 "Sending your link…", L551 "Signing you in…", L718 truncation, L734 "Loading your people…", L849 "Search your people…", L991 "Preparing…", L1026/1431/1490 "Saving…".
- `_capture.js` L244 "Reading…", L317,644 "Saving…".
- `_memory.js` L172 "Something worth remembering…", L189 "Reading…", L230 "Saving…", L249/276 "Removing…".
- `import.js` L96,238,334,423,522.
- `roster.js` L119,213,266.
- `index.html` L1207,1233,1247,1249(also em-dash — reword),1306,1321,1712,1747,2025,3416,3417,4242.
- `_capture.mjs` (server) L300 UI string em-dash — reword. (Rest of _capture.mjs = comments/prompts, out of scope.)
- LEAVE index.html L4014 `.replace(/…/g,'...')` — that's the OUTPUT sanitizer for user-sendable copy (separate concern).

## 5. Prose / simplification (Airbnb-card-led + Apple-restraint)
- **Hero L659-690:** H1 L660, ~45-word lead L661, "you'll get" line L662, orb front door L670-680, reassure L689 — centered/prose-heavy → shorter, tighter, card-led.
- **"How it works" + occasion chips L700-733:** occasions live as CHIPS L720-730 BELOW prose (section head L701 + sub L702 + 3-step grid L703-719). 9 chips: New baby, New job or promotion, Engagement or wedding, Graduation, Birthday, Thank-you just because, A hard diagnosis, Lost a loved one, Just a hard week → PROMOTE into card-led hero. `.moments .chip` L207-209; `.steps/.step` L200-205.
- **Real-example card L736-747;** quote band L749-754; **mission prose to cut ~4→1:** footer block L757-762 + subs L702/L745.

## 6. Marketing OS tenant logo (SEPARATE repo — PROPOSE, do not auto-apply)
Repo `C:\Users\David\Damay Enterprises\marketing-os`. TC = live tenant.
- App icon/avatar: `marketing-os/public/tc-app-icon.png` (512×512 cream "tc") — REPLACE.
- Tenant config: Supabase `apps` row `id='thoughts-count'`, `brand` JSONB has `voice`, NO `logo_url`. Schema `sql/001-schema.sql` L42-43.
- Wiring: `lib/engine/brand-config.js` ~L135 reads `logo_url`; `graphics_base_url=https://thoughts-count.netlify.app`.
- Per-brand cards: `marketing-os/public/cards/thoughts-count/*.png` (9) + `ideas/thoughts-count/*.json`.
- **Actions (route orchestrator→David, shared-infra guardrail):** replace `tc-app-icon.png`; optional `UPDATE apps SET brand=brand||jsonb_build_object('logo_url',…)` (PROD DB — approval); regenerate 9 cards; redeploy marketing-os. Its own ticket w/ sign-off.

## 7. Risk / collisions
- **Della's orb = highest risk.** Hand-tuned canvas; 3 mounts (home L670, modal `#cvOrbStage`, CSS halo/breath L449/L822). Re-palette is design+build collab; verify all 3 mounts + reduced-motion still frame.
- **No central token layer** — introduce tokens FIRST, re-point all literals + fallbacks, THEN palette = one place. Otherwise a color is missed.
- **Font swap layout risk** — Fraunces serif → Montserrat changes metrics; check every heading/`max-width:Nch` + `.serif` italic sites (24 in index.html) for reflow/overflow.
- **Guides = generator; edit ONCE** then re-run to rewrite 22 files + sitemap.
- **Easy-to-forget:** emails (Georgia + emoji, no SVG); privacy/terms (own `:root`, var names `--deep`/`--cream`); `@media print` plan printout (index.html L537-538); error states (L378/391); loading/"thinking it through" states; empty states; `og.png` + `favicon.svg` binaries; Marketing-OS assets (§6).
- **Netlify redirect index.html L6-11** (netlify.app→thoughtscount.com) — magic-link auth depends on it; don't remove.
- **`HER_NAME`/persona swappable** (`_persona.mjs` L12) — do NOT rename Della in this rebrand.

**UX gate: MUST run**, walking the natural user path (home → talk by voice → intake → plan → save/People → email), owning the FULL experience/coverage, not just the diff.
